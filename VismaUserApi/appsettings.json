{
  "ConnectionStrings": {
    "VismaChallenge": "Server=(localdb)\\MSSQLLocalDB;Database=VismaChallenge;persist security info=True;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "PasswordOptions": {
    "SaltSize": 16,
    "KeySize": 16,
    "Iterations": 10000
  },
  "Authentication": {
    "SecretKey": "VISMA_CHALLENGE_KEY", // el key
    "Issuing": "http:localhost:4433", //when is running the API (optional)
    "Audience": "http:localhost:4433" // when is running the client (optional)
  }
}
